extends ../layouts/default.pug
include ../mixins/option.pug

block main
  if (permissionList.includes("article-category-create"))
    .card
      .card-header
        .row.align-items-center
          .col
            h4.card-title #{pageTitle}
      .card-body.pt-0
        form(id="articleCreateCategoryForm")
          .row
            .col-md-6.col-lg-6
              .mb-3
                label.form-label(for="name") Tên danh mục *
                input.form-control(id="name" type="text" name="name")
            .col-md-6.col-lg-6
              .mb-3
                label.form-label(for="slug") Đường dẫn *
                .input-group
                  span.btn.btn-outline-info(
                    btn-generate-slug
                    model="CategoryBlog"
                    from="name"
                    to="slug"
                    ) Tự động
                  input.form-control(id="slug" type="text" name="slug")
            .col-md-6.col-lg-6
              .mb-3
                label.form-label(for="parent") Danh mục cha
                select.form-select(id="parent" name="parent")
                  option(value="") --Chọn danh mục--
                  +option(categoryList)
            .col-md-6.col-lg-6
              .mb-3
                label.form-label(for="status") Trạng thái
                select.form-select(id="status" name="status")
                  option(value="active") Hoạt động
                  option(value="inactive") Tạm dừng
            .col-md-6.col-lg-6
              .mb-3
                div(form-group-file)
                  label.form-label(for="avatar") Ảnh đại diện
                  .input-group
                    span.btn.btn-outline-info(
                      btn-file-manager
                      data-bs-toggle="modal" 
                      data-bs-target="#modalFileManager"
                      ) Chọn file
                    input.form-control(id="avatar" type="text" name="avatar" input-file placeholder="Đường dẫn file...")
                  .preview-file(preview-file)
                    img
            .col-md-12.col-lg-12
              .mb-3
                label.form-label(for="description") Mô tả
                textarea.form-control(id="description" name="description" textarea-mce)
          button.btn.btn-primary(type="submit") Tạo mới
          a.btn.btn-danger.ms-1(href=`/${pathAdmin}/article/category`) Hủy
