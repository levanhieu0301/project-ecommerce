extends ../layouts/default.pug
include ../mixins/pagination.pug

block main
  if (permissionList.includes("file-manager"))
    iframe(
      src=`/${pathAdmin}/file-manager`, 
      frameborder="0"
      style="width: 100%; height: 76vh;"
    ) 
